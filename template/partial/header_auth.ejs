<ul class="nav justify-content-end nav_top">
    <a 
        class="nav-link disabled" 
        href="#" 
        tabindex="-1" 
        style="left: 5px; position: absolute;"
        aria-disabled="true">
        BrokenBox
    </a>
    <li class="nav-item">
        <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Welcome <%=username%></a>
    </li>
    <li class="nav-item">
        <a class="nav-link" onclick="confirmReset()" data-toggle="tooltip" title="Reset">
            <i class="fa fa-power-off fa-lg"></i>
        </a>
    </li>
    <li class="nav-item">
        <a class="nav-link" onclick="confirmLogout()" data-toggle="tooltip" title="Logout">
            <i class="fa fa-sign-out fa-lg"></i>
        </a>
    </li>
</ul>

<script>
    function confirmReset(){
        var ask = window.confirm("Are you sure you want to reset your database ?");
        if (ask) {
            window.location = "/reset";
        }
    }

    function confirmLogout(){
        var ask = window.confirm("Are you sure you want to logout ?");
        if (ask) {
            window.location = "/logout";
        }
    }
</script>